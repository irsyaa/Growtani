const typedOptions = {
    strings: ['Nice to Meet You ', 'Have a Great Day'],
    typeSpeed: 50,
    backSpeed: 20,
    loop: false,
    startDelay: 85,
  };
  
  const typed = new Typed('.typed-here', typedOptions);
  